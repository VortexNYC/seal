{
	"buildCommand": "cd apps/backend && bunx convex deploy --cmd 'bunx turbo run build --filter=@seal/web'",
	"devCommand": "cd apps/backend && bunx convex deploy --cmd 'bunx turbo run dev --filter=@seal/web'",
	"installCommand": "bun install",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/index.html"
		}
	]
}
