# Production build defaults for the Seal web app.
# These are public URLs only; secrets are injected at deploy time.
VITE_API_URL=https://api.seal.nyc
VITE_BETTER_AUTH_URL=https://api.seal.nyc
VITE_APP_URL=https://app.seal.nyc
