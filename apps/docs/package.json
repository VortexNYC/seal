{
  "name": "@seal/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "blume dev",
    "build": "blume build",
    "doctor": "blume doctor",
    "deploy": "pnpm run build && wrangler deploy -e production",
    "preview-deploy": "pnpm run build && wrangler versions upload -e production --preview-alias"
  },
  "dependencies": {
    "blume": "^1.7.0"
  },
  "devDependencies": {
    "wrangler": "^4.133.0"
  }
}
