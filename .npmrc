# Registry mapping only. Authentication is configured at user level because pnpm 11
# ignores a committed project-level _authToken assignment.
strict-peer-dependencies=false
auto-install-peers=true
@vortexnyc:registry=https://npm.pkg.github.com
