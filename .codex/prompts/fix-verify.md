---
description: Run verify and fix all issues without suppression
---

Run `bun run verify`, fix all reported issues, and do not ignore or suppress errors.

Requirements:

- Address root causes instead of bypassing rules.
- Keep fixes minimal and scoped.
- Re-run verify until it passes cleanly.
