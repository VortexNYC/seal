<!-- Generated by @vortexnyc/tooling vortex-project-kit. Edit vortex.project.json, not this file. -->

# Seal LLM Integration Packet

## Source Of Truth

- Project: `seal`
- Package: `seal`
- Base branch: `staging`
- Package manager: `bun@1.3.11`
- Convex deployment: `dev:aware-buzzard-568`

## First Actions

- Run `bun run repo:status` from the repo root.
- Read AGENTS.md and any subdirectory AGENTS.md before touching code.
- Read .test-env before live proof or dev-server work.
- Attack the single active limiter; do not broaden payments, auth, or docs scope without evidence.

## Required Commands

Setup:

```bash
bun install
```

Proof wall:

```bash
bun run project-kit:check
bun run project-kit:doctor
bun run migrate:legacy-auth-package:check
bun run typecheck
bun run lint:strict
bun run format:changed:check
bun run check:preferred-stack
bun run build
bun run test
bun run auth:check
bun run auth:preflight
```

Production release proofs:

No generated commands. Add `instructions.verificationCommands` to `vortex.project.json`.

## Vortex Core Integration

- Auth package: `@vortexnyc/auth`
- Convex auth directory: `apps/backend/convex`
- Required auth checks: `bun run auth:check`, then `bun run auth:preflight`
- App code owns adapter glue, local anchor tables, routes, and product copy.
- Core owns auth semantics, hosted auth UI, component truth, API auth, webhooks, MCP helpers, and preflight checks.

Use Core-owned tooling through the repo scripts generated by `vortex-project-kit`.
Do not create consumer-local copies of Vortex Auth, lint, format, repo hygiene,
React Doctor, or Convex cost policy.

## App Boundary

Consumer owns:

- domain schemas
- product vocabulary
- app-specific permission catalogs
- deployment identifiers
- test accounts
- route layout
- brand copy

Core owns:

- shared auth/runtime semantics
- reusable hosted auth/account/org/API-key/security surfaces
- consumer contract checks
- package version policy
- project instructions
- preferred stack checks
- Convex operational script names

## Apps

- apps/web - React 19 Vite product app with TanStack Router, Better Auth, Convex, and Cloudflare deploys
- apps/landing - TanStack Start/Fumadocs marketing and developer docs site
- apps/backend - Convex backend and auth/payment/API surface
- apps/mcp-worker - Cloudflare Worker MCP server

## Packages

- packages/transactional - React Email templates
- packages/react-sdk - embeddable signing components
- packages/tokens - shared Seal design tokens
- packages/vortex-payments - Vortex payments adapter package

## Forbidden Touch Zones

- apps/backend/convex/\_generated/\*
- apps/web/src/routeTree.gen.ts
- apps/landing/src/routeTree.gen.ts
- apps/landing/.source/\*
- apps/landing/content/docs/api-reference/\*
- apps/backend/convex/schemas/subscription_coupons.ts
- apps/backend/convex/schemas/subscription_promo_codes.ts

## Dev URLs

- http://seal.localhost:1355
- http://seal-landing.localhost:1355
- https://staging-app.seal.nyc
- https://app.seal.nyc
- https://aware-buzzard-568.convex.cloud
- https://aware-buzzard-568.convex.site
- https://clever-goose-484.convex.cloud
- https://clever-goose-484.convex.site

## Local Notes

- Read .test-env before live proof; it is the local source of truth for dev URLs and deployments.
- Use vb or vb --fresh against TEST_WEB_URL for browser proof.
- Do not reopen payments migration scope unless the active task explicitly requires it.

## Cloud Notes

- Preserve root AGENTS.md and CLAUDE.md because they contain Seal-specific navigation and design context.
- Use staging for Stripe Connect, subscriptions, webhooks, billing, and payment verification.
- Do not target the retired self-hosted Convex URLs.
