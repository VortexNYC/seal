# ═══════════════════════════════════════════════════════════════
# SEAL — Test Environment Config
# This file is the SINGLE SOURCE OF TRUTH for all agent testing.
# Agents: read this file. Do not search 1Password. Do not guess.
# ═══════════════════════════════════════════════════════════════

# ── Platform ──
PLATFORM=web
TESTING_TOOL=agent-browser

# ── Test Account (primary) ──
TEST_EMAIL=shlomo@seal.nyc
TEST_PASSWORD=12345678
TEST_CLERK_USER_ID=user_3B4mXADQ8bT4N52DGjRCRo49PNZ

# ── Clerk (self-service) ──
CLERK_SECRET_KEY_1P_ID=aui4r5xvacv4ax5f7hkiayc6jm

# ── Stripe ──
STRIPE_TEST_KEY_1P_ID=nfxwzgmfxiypaoqfel7li3lsiu

# ── Convex ──
CONVEX_URL=https://coordinated-lemur-768.convex.cloud
CONVEX_SITE_URL=https://coordinated-lemur-768.convex.site
CONVEX_DEPLOYMENT=dev:coordinated-lemur-768

# ── Sentry ── (MANDATORY)
SENTRY_ORG=vortex-nyc
SENTRY_PROJECT=seal

# ── PostHog ── (MANDATORY)
POSTHOG_KEY=phc_SnB4NSisivT3pLSYoXq17RMixLU0oTfBFqfoNxwn64P
POSTHOG_HOST=https://us.i.posthog.com

# ── Logging ──
# Use openlogs (ol) for all dev server logs: ol bun dev
# Logs at: .openlogs/latest.txt

# ── App URLs (portless) ──
TEST_WEB_URL=http://seal.localhost:1355
TEST_LANDING_URL=http://seal-landing.localhost:1355

# ── Dev Commands (ALL through portless + openlogs) ──
DEV_ALL="cd /Users/shlomokabareti/projects/Seal && portless seal ol bun dev"
