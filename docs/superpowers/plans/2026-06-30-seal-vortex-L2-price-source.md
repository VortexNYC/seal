# Seal SaaS Price Source → Vortex (#13 L2, MINIMAL) Implementation Plan

> Execution: Claude PLANS+REVIEWS+GATES, Codex EXECUTES (`codex exec … < /dev/null`, no git — Claude commits). Repo: Seal `/Users/shlomokabareti/projects/Seal`. Base: branch `feat/seal-vortex-sdk-adoption-l2` from `feat/seal-vortex-sdk-adoption-l1` (depends on L1's SDK transport) OR from `staging` after L1 merges.

**Goal:** Sever the last retired provider coupling on the SaaS-subscription **Vortex** path: the price-validation guard at `subscription_actions.ts:104-111` requires a retired provider-synced `subscription_prices` row even for allowlisted (`vortex_billing`) orgs. Move it into the retired provider-only branch so the Vortex path validates the lookupKey solely against the Vortex price catalog (`VORTEX_BILLING_SAAS_PRICE_MAP`). Then seed the Vortex price catalog + document the env maps. **Behavior-preserving on both branches.**

**Why minimal (scout 2026-06-30):** Customer provisioning is ALREADY implicit on the Vortex branch (Vortex auto-provisions from `customerExternalId` sent at checkout; the retired provider-customer-create `resolveOrgBillingCustomer` is unreachable on that branch). `activeretired providerIdPresent:false` ALREADY holds (computed from projected subscription rows' Vortex-shaped ids in `vortex_billing/projection.ts:88-105`, not `organizations.retired_providerCustomerId`). Explicit `upsertCustomer` is redundant for provisioning → deferred. The ONLY residual retired provider coupling on the Vortex path is the pre-branch price guard.

## Global Constraints
- **Codex L2 review (2026-06-30): SOUND-TO-BUILD after this amendment.** Confirmed: `getPriceByLookupKey` is `internalMutation` in name only — pure query, NO write side effect (safe to skip on Vortex branch); unknown Vortex lookupKey still fails closed via `resolveVortexBillingConfig`; no other caller depends on the reorder. Amended: seed script uses the generated SDK wrapper shape (finding 4).
- Behavior-preserving. **retired provider branch FUNCTIONALLY identical** (still requires the retired provider price + `externalPriceId`). Note (finding 3): the reorder calls `selectSaasBillingProvider` before the retired provider price guard, so under a MALFORMED `VORTEX_BILLING_SAAS_ORGANIZATION_IDS` a retired provider org with a missing price could surface the allowlist-parse error first instead of "Price not found" — acceptable (not real drift under sane env), but not literally byte-identical. **Vortex branch**: a `vortex_billing` org with NO retired provider `subscription_prices` row must succeed if its lookupKey is in `VORTEX_BILLING_SAAS_PRICE_MAP`; an unknown lookupKey must still throw (today via `resolveVortexBillingConfig` `vortex_billing_processor.ts:93-95` "Vortex Billing price missing for lookup key").
- Do NOT touch `vortex_billing_processor.ts` (L1), `selectSaasBillingProvider`, the allowlist, or the webhook projection.
- No `any`, no eslint-disable. `bun`/`bunx`. Gate from Seal ROOT: typecheck, lint, test (export `NODE_AUTH_TOKEN` from 1Password "GitHub NODE_AUTH_TOKEN (vortexnyc read:packages — agent installs)" before any install/gate that resolves deps).

## File Structure
- `apps/backend/convex/payments/subscription_actions.ts:100-132` — **modify**: move the `getPriceByLookupKey` call + `if (!priceData?.price) throw` from before the provider branch (`:104-111`) into the retired provider branch (after the `=== "vortex_billing"` early-return at `:120`). The Vortex branch no longer depends on a retired provider price row.
- `apps/backend/convex/payments/subscription_actions.test.ts` (or the existing test for this action) — **modify/create**: Vortex-path test with no retired provider price row → checkout succeeds; Vortex-path unknown lookupKey → throws PRICE_MAP error; retired provider-path missing price → still throws "Price not found".
- `apps/backend/scripts/seed-vortex-saas-catalog.ts` (or `convex/` internal action) — **create**: one-time `createProduct` + `createPrice` (`priceType: "fixed_recurring"`) per SaaS plan, producing the Vortex priceIds that `VORTEX_BILLING_SAAS_PRICE_MAP` references. Uses `@vortexnyc/payments-sdk` `createClient` + `createProduct`/`createPrice` (confirmed in SDK d.ts).
- `apps/backend/.env.example` — **modify**: document `VORTEX_BILLING_SAAS_ORGANIZATION_IDS`, `VORTEX_BILLING_API_BASE_URL`, `VORTEX_BILLING_API_KEY`, `VORTEX_BILLING_ACCOUNT_ID`/`_MAP`, `VORTEX_BILLING_SAAS_PRICE_MAP`, `VORTEX_BILLING_CUSTOMER_MAP` (currently undocumented).

## Task 1: Move the retired provider price guard into the retired provider branch (TDD)
**Files:** `subscription_actions.ts`, its test.
- [ ] Step 1 (test-first): add a test where `selectSaasBillingProvider` returns `vortex_billing` (allowlist the org) and `getPriceByLookupKey` returns no price → assert checkout STILL succeeds (mock `createVortexBillingCheckoutSession`/the Vortex transport). Confirm it FAILS today (guard throws "Price not found" before the branch).
- [ ] Step 2: implement — read `subscription_actions.ts:100-132`; compute `selectSaasBillingProvider(organization._id)` first; on `vortex_billing` → call `createVortexBillingCheckoutSession` + return (no `getPriceByLookupKey`); only AFTER the early return, do `const priceData = await getPriceByLookupKey(...)` + `if (!priceData?.price) throw` for the retired provider branch. Keep the retired provider branch byte-identical otherwise.
- [ ] Step 3: run the test → PASS. Add/keep: retired provider-path-missing-price still throws; Vortex unknown-lookupKey throws (via resolveVortexBillingConfig).
- [ ] Step 4: gate (typecheck/lint/test from root) + Claude commits `refactor(billing): source SaaS price from Vortex catalog on the vortex_billing branch (drop retired provider price-row dependency)`.

## Task 2: Seed Vortex price catalog + document env maps
**Files:** seed script, `.env.example`.
- [ ] Step 1: write `seed-vortex-saas-catalog.ts` — build the client like L1: `const client = createClient({ baseUrl: trimTrailingSlash(apiBaseUrl), headers: { authorization: \`Bearer ${apiKey}\`, "x-vortex-service": "billing" } })`. For each SaaS plan/lookupKey use the GENERATED SDK WRAPPER SHAPE (Codex L2 finding 4 — NOT positional args): `await createProduct({ client, headers: { "Idempotency-Key": ... }, body: { productId, name, description?, metadata? } })` then `await createPrice({ client, headers: { "Idempotency-Key": ... }, body: { priceId, productId, name, priceType: "fixed_recurring", currency, billingInterval, billingIntervalCount, unitAmount } })`. Both return `{ data, error, response }`; treat an existing-catalog conflict as **HTTP 409** (idempotent skip) unless the live sandbox proves another status — do NOT assume 411. Print the resulting priceIds to populate `VORTEX_BILLING_SAAS_PRICE_MAP`.
- [ ] Step 2: document all `VORTEX_BILLING_*` env keys in `.env.example` with example shapes (allowlist JSON array, price map JSON `{lookupKey:vortexPriceId}`, customer map JSON).
- [ ] Step 3: gate (typecheck/lint) + commit `chore(billing): seed Vortex SaaS price catalog + document VORTEX_BILLING_* env`.

## Task 3: Codex review + push
- [ ] Codex diff review (behavior drift: retired provider branch unchanged; Vortex branch no longer needs a retired provider price row; unknown lookupKey still throws).
- [ ] Push to `staging` (Shlomo gate) → PR → Seal CI green.
- [ ] Update memory [[vortexnyc-payments-sdk-l0-published.md]]: L2 DONE; NEXT = L3 (cancel/pause/resume/update_items/portal → Vortex via SDK `changeSubscription` + hosted portal; `subscription_actions.ts:139-194` retired provider-only today).

## Self-Review
Coverage: decouple price guard (T1) ✓; seed catalog + docs (T2) ✓; review+push (T3) ✓. Customer provisioning/`activeretired providerIdPresent` already hold (no task needed — scout-verified). Residual risk for Codex: confirm `getPriceByLookupKey` has no SIDE EFFECT relied upon elsewhere when skipped on the Vortex branch (it's a read; the scout shows it only validates+returns the local price row) — verify in T1 Step 2.
