# Seal → vortex-auth migration runbook

> ## ▶ MCP OAuth cutover — IN PROGRESS (2026-06-02): the LAST Clerk in Seal
> **DECISION (supersedes the old "repoint vs retire the worker" fork): Topology 2 — MCP rides the existing `/api/v1` resource server.** Seal's 49 MCP tools are 1:1 wrappers over `/api/v1` REST, and `/api/v1` is already component-backed (P5). So we do NOT clone 49 in-Convex `/mcp` handlers (idiot-index violation) and we do NOT keep a fat Clerk worker. Instead: `/api/v1` validates the Better-Auth MCP token via the package resolver; the worker becomes a thin pass-through; the dedicated `/mcp` dispatch is deleted. crm keeps a worker too, so "worker + Convex" is NOT a divergence.
>
> **DONE + dev-deployed + verified (negative paths) on `aware-buzzard-568`:**
> 1. ✅ `api/context.ts`: new `resolveMcpApiAuth` (verify via `internal.mcpOAuthNode.verifyAccessToken` → `resolveMcpAuth` → `buildAuthContext`, so MCP obeys the same org-security gating as API keys). `resolveAuthContext` routes ES256 JWTs → it. **DELETED** the entire Clerk JWT path (`resolveJwtAuth`, `resolveOAuthAuthContext`, `verifyOAuthAccessToken`, `@clerk/backend verifyToken` import, `ClerkJwtPayload`, `isOAuthAccessToken`, `requireMcpScope`). `ApiAuthType` is now `api_key | mcp_oauth`. `requireScope`/`requireAnyScope` give `mcp_oauth` scope-first-with-role/permission-fallback; api_key stays strict. Bug found+fixed in dev test: invalid token threw → 500; now caught → 401.
> 2. ✅ `http.ts`: deleted the 3-stub `POST /mcp` dispatch + helpers + the route (kept the OAuth *server* routes: metadata/jwks/authorize/token/register). `MCP_OAUTH_AUDIENCE`/`MCP_OAUTH_RESOURCE_ID` moved to `api/context.ts` (leaf) and re-exported from `mcpOAuth.ts` (kills an import cycle).
> 3. ✅ `apps/mcp-worker`: thinned to a Clerk-free pass-through — removed `@clerk/backend` + `@clerk/mcp-tools` (deps + code), proxies Seal's live protected-resource metadata, forwards the bearer to `/api/v1` (49 tools + `SealApiClient` UNCHANGED). Worker typechecks. **Still points at PROD `compassionate-robin-742` (wrangler.jsonc) and is deployed at `mcp.seal.nyc` — NOT redeployed yet.**
> - Verified on dev: invalid token→401 `INVALID_JWT`, missing header→401, junk key→401, old `/mcp`→404, OAuth metadata→200, dynamic client register→201, authorize correctly demands a session (`login_required`) + org binding. backend typecheck/lint/17-test green; worker typecheck green.
>
> **POSITIVE E2E — ✅ PROVEN GREEN on dev (2026-06-02):** full real-token flow — dynamic client register → `authorize` (302+code, with `organization_id`) → `token` (ES256 access token) → **`GET /api/v1/documents` → 200 `{"data":[],"has_more":false}`**. A real Better-Auth MCP OAuth token (no Clerk) is accepted by `/api/v1` via `resolveMcpApiAuth` → user+org+scopes → data. Dev artifacts left: user `seal-mcp-e2e@seal.nyc` (pw `McpE2ePassword123!`), org `mcp-e2e` (`k5738…`), a dynamic client.
>
> **✅ GAP CLOSED (2026-06-02) — fresh orgs now anchor into the component at creation.** Added `anchorNewOrganizationOwner` (`lib/vortexAuthOrganizations.ts`): ensures the org anchor + seeds the component role catalog + mirrors the owner membership. Wired into BOTH app-side org-create mutations — `ensurePersonalOrganization` (onboarding) and `createWorkspace` (`organizations/mutations.ts`). (The better-auth glue path `canonicalGlue.ts:145` already anchored.) **Proven green with a BRAND-NEW user, zero manual steps:** signup→onboard→workspace auto-anchored (56 perms incl `documents:read`) → authorize 302 → token 200 → `GET /api/v1/documents` 200, templates 200. Idempotent; guarded on the owner being bridged (always true post-signup). Was the root cause of the earlier `invalid_scope` (`auth.ts:260` lazy mirror only fires on org-scoped mutations + needs the anchor first; the backfill migration is one-shot and missed new orgs).
>
> **✅ PROD CUTOVER COMPLETE + PROVEN GREEN (2026-06-02) — the WHOLE migration is live on prod.** Fast-tracked direct to prod `compassionate-robin-742` (pre-launch, only ~13 internal/test Clerk users, no real customers; they re-signup). Sequence executed:
> - Set prod `BETTER_AUTH_*` env (issuer/jwks/url = compassionate-robin-742 site; fresh secret; `BETTER_AUTH_TRUSTED_ORIGINS=https://app.seal.nyc`).
> - **Prod data migration (re-add-strip-redrop):** the first `convex deploy` was rejected — prod `organization_members` rows still carried `clerkMembershipId` (P7 dropped it on dev only). Re-added the field optional → deployed → ran `migrations:stripOrgMemberClerkMembershipId` (13 rows) → removed field+migration → clean redeploy. (Orphaned tables `waitlist_entries`/`mcp_oauth_*` still exist on prod but are extra-tables-not-in-schema → harmless, didn't block. Optional later cleanup.)
> - Deployed all 3 surfaces (all CF/Convex, deploy mechanism: Convex `deploy`, CF `wrangler deploy` with the Vortex CF token): backend → prod; **frontend `seal-web` → `app.seal.nyc`** (the web app is a CF Worker serving the SPA; built with `VITE_BETTER_AUTH_URL` added, Clerk key dropped); **worker `seal-mcp` → `mcp.seal.nyc`** (thinned, Clerk-free). Note: prod web app = `app.seal.nyc`; `seal.nyc` = landing.
> - **Prod E2E GREEN:** web signup on app.seal.nyc → onboard (auto-anchored, 56 perms) → register client → authorize 302 → token 200 → `GET /api/v1/documents` 200; `mcp.seal.nyc` MCP `initialize` 200 + unauth→401 with correct `WWW-Authenticate`. OAuth metadata/JWKS/`/api/auth/ok` all 200 on prod.
> - **Clerk teardown DONE:** removed `@clerk/backend` from `apps/backend` (import-free) + `bun install`; removed `CLERK_*` env from prod (only `BETTER_AUTH_*` remains); clean redeploy green.
>
> **REMAINING (non-blocking):** dev `aware-buzzard-568` is consistent (was already clean). The branch is UNCOMMITTED (runner owns git). Future: **P8 paperwork** (PR `feat/vortex-auth-p0-scaffold` → `staging` to land it in git + fix stale `.test-env` + `emailVerification.required:true`), optional prod cleanup of orphaned `waitlist_entries`/`mcp_oauth_*` tables, and the cosmetic `clerkId`→`authSubject` rename. Dev/prod test artifacts: users `seal-mcp-e2e@seal.nyc`, `seal-mcp-e2e2@seal.nyc` (dev), `seal-prod-mcp@seal.nyc` (prod) + dynamic OAuth clients.
> NOTE: `users.clerkId`/`organizations.clerkId` are KEPT (functional auth-subject key, ~30 sites) — later cosmetic rename→`authSubject`, not a drop.

**Goal:** Completely replace Clerk with `@plasmapos/vortex-auth` (Better-Auth + Convex), test backend + UI, then remove Clerk entirely. End state matches **crm/plasma** (B2B, component-truth). Seal is web-only (Vite) + Convex.

**Branch:** `feat/vortex-auth-p0-scaffold` off `staging` (Seal base = `staging`).
**Decision:** Waitlist is **DROPPED** (remove `<Waitlist>` + `waitlistEntry.*` webhooks + `waitlist_entries`).
**Email:** `@seal/transactional` + Resend, `seal.nyc` Resend domain VERIFIED. Use the package's shipped builders (`createEmailVerificationEmailDraft`/`createPasswordResetEmailDraft` from `@plasmapos/vortex-auth/convex`) — do NOT hand-roll (pile's lesson).
**Deployments (Convex Cloud):** dev `aware-buzzard-568`, prod `compassionate-robin-742`. `.test-env` is STALE (points at dead self-hosted seal-staging) — fix in P8.

## Execution rules (per pile/crm/plasma)
- Each phase is ADDITIVE and must `typecheck` green before the next. Clerk is torn out (P6) only AFTER vortex-auth is proven live.
- Copy/adapt the **crm blueprint** (B2B, proven): `~/projects/crm/convex/{convex.config.ts, betterAuthClient.ts, auth.config.ts, betterAuth.ts, lib/canonicalGlue.ts, lib/authIdentities.ts}` and crm `users.ts` (`upsertFromBetterAuth`/`deleteFromBetterAuth`).
- Keep Seal app-side: org anchor + rich org fields (branding/signing/security/ai/stripeCustomerId), the 5-tier roles + ~40 permissions + custom roles (`auth.utils.ts`). Component owns membership/role/invitation/api-key TRUTH.
- Run from `apps/backend`: `bunx convex dev --once` to codegen after component/schema changes. Test UI via dev-browser at `http://seal.localhost:1355` (`portless seal ol bun run dev`).

## Phases
- **P0 — scaffold (additive):** deps in `apps/backend/package.json` (`@plasmapos/vortex-auth@0.1.172`, `@convex-dev/better-auth@0.12.2`, `better-auth@1.6.9`; resend already present). Mount `betterAuth` + `vortexAuth` in `convex.config.ts`. Create `betterAuthClient.ts` (createClient seam + sync triggers → `internal.users.upsertFromBetterAuth`/`deleteFromBetterAuth` + `components.vortexAuth.identity.provisionFromIdentity`), `betterAuth.ts` (runtime, `twoFactor:{enabled:true,issuer:"Seal"}`, rateLimit, trustedOrigins incl. `http://seal.localhost:1355` + staging/prod app origins), `lib/canonicalGlue.ts` (`createVortexAuthGlue` orgs:enabled, B2B adapters for Seal schema), `lib/authIdentities.ts`. Make `auth.config.ts` ADDITIVE (keep Clerk provider AND add `betterAuthConvexProvider`). `registerAuthRoutes(http)` in `http.ts`. Schema (additive optional): `users.vortexAuthUserId` + `by_vortex_auth_user` index, `users.activeVortexAuthOrganizationId`, `organizations.vortexAuthOrganizationId` + `by_vortex_auth_organization` index. Add `internal users.upsertFromBetterAuth` + `deleteFromBetterAuth`. `convex dev --once` + typecheck.
- **P1 — identity bridge:** verify user-sync provisions Seal `users` from Better-Auth signup; both providers validate.
- **P2 — component-truth:** route orgs/members/roles/invitations/api-keys through `vortexAuth` component (crm pattern); backfill existing Clerk-synced data into the component; keep app fields app-side.
- **P3 — auth email:** wire Better-Auth `sendEmail` to shipped builders / `@seal/transactional`.
- **P3 — DONE** (commit `af74fa0`): `betterAuth.ts` now wires the runtime `sendEmail` seam (renders via shipped `createEmailVerificationEmailDraft`/`createPasswordResetEmailDraft`, sends via Seal's Resend component) + `emailVerification:{sendOnSignUp:true, required:false}`. VERIFIED live: signup → "Verify your email" sent (Resend status "sent", real message id). Tighten `required:true` at P8.
- **P4 — frontend swap (web). FOUNDATION DONE (commit `ca6fcea`, additive, live Clerk path untouched):** `users.getCurrentUser` query + `apps/web/src/lib/auth-runtime.better-auth.tsx` (Seal's `createVortexBetterAuthRuntime` provider + `useAppAuth`/`useAppUser`/`useAppAuthActions`, mirrors crm) + `@plasmapos/vortex-auth` web dep. backend+web tsc clean.
  - **P4 CUTOVER — TODO (breaking; do as ONE dev-browser-tested unit, needs `VITE_BETTER_AUTH_URL` env = `<convex-site>/api/auth`):** (1) `main.tsx`: replace `ThemedClerkProvider`+`ConvexProviderWithClerk` with `AuthRuntimeProvider` from the new runtime; drop `VITE_CLERK_PUBLISHABLE_KEY` guard. (2) Auth UI routes → `vortex-auth/react` components: `_auth/sign-in.tsx` (`<SignIn>`), `sign-up.tsx` (`<SignUp>`), `_authenticated/.../settings/profile/index.tsx` + `security.tsx` (`<UserProfile>` + 2FA), `_auth/accept-invite.tsx`, `onboarding/choose-organization` (`<TaskChooseOrganization>`). (3) Hooks: `useUser`→`useAppUser`, `useOrganization`/`useClerk`→app-auth equivalents across ~15 files (app-sidebar, _authenticated.tsx, app.tsx, posthog-identify, use-jam-metadata, members-list, document-presence, error boundaries, etc.). (4) DELETE `lib/clerk-auth-theme.ts`. (5) **REMOVE `_auth/waitlist.tsx` route** (decision: waitlist dropped). (6) dev-browser test on `seal.localhost:1355`: sign-up→verify-email→sign-in→org→2FA enroll/step-up→accept-invite→sign-out, BOTH light+dark. crm refs: `apps/web/src/lib/auth-runtime.better-auth.tsx`, `routes/_authenticated/-post-sign-up.logic.ts`, `lib/invite-sign-up.ts`, `settings.security.tsx`.
- **P5 — API keys → component:** `api/context.ts` `verifyToken` (Clerk) → vortexAuth component API keys (crm proved it).
- **P6 — Clerk teardown:** drop `@clerk/*` (fe+be); delete `clerk_webhooks.ts` (~1033 lines) + `/clerk-webhooks` route + handlers in `http.ts`; remove `createClerkClient` usages (5 files) + `recoverOrganizationSyncFromClerk`; remove Clerk provider from `auth.config.ts`; remove all `CLERK_*` env; drop waitlist tables/webhooks.
- **P7 — schema cleanup:** drop `clerkId` (users, organizations), `clerkMembershipId`/`externalId` (members), `clerkInvitationId`/`clerkOrganizationId` (invitations) + indexes via add→backfill→switch→drop (+5th re-add-strip-redrop safety if Convex rejects live deploy). Convert `organization_invitations.token` from Clerk-id to Convex-generated.
- **P8 — full-stack test + cleanup:** backend (`convex run` + logs) + UI E2E via dev-browser on `seal.localhost:1355`: signup → org create → invite → accept → 2FA enroll/step-up → API key → sign-out. Verify on dev `aware-buzzard-568`. Fix stale `.test-env` (self-hosted → Cloud). Sentry sweep. Then PR → `staging`.

## Progress
- **P0 — DONE** (commit `809d764`, branch `feat/vortex-auth-p0-scaffold`). Scaffold landed + deployed clean to dev `aware-buzzard-568` (both components installed). Files: convex.config (betterAuth+vortexAuth), betterAuthClient.ts, auth.config.ts (additive — Clerk + Better-Auth), betterAuth.ts, lib/canonicalGlue.ts, lib/authIdentities.ts, users.ts (upsert/deleteFromBetterAuth), http.ts (registerAuthRoutes), schema additions. Verified: tsc clean, lint 0 err, 1368/1368 tests, convex dev --once OK.
  - GOTCHA 1: `@plasmapos` is on **GitHub Packages**, not public npm. Added `.npmrc` (`@plasmapos:registry=https://npm.pkg.github.com` + `${NODE_AUTH_TOKEN}`). Install/codegen/deploy all need `NODE_AUTH_TOKEN=$(gh auth token)` (gh token has write:packages).
  - GOTCHA 2: backend `typecheck` used `@typescript/native-preview`, which **panics** on the 23k-line component-generated api.d.ts. Switched to stock `tsc` (matches every other Seal pkg + crm).
  - DECISION: `users.clerkId` kept **REQUIRED** (not optional) to avoid churning ~11 clerkId-keyed call sites; Better-Auth users store their Better-Auth subject in clerkId transitionally (unique, no `user_…` collision). Whole column dropped in P7.
  - Env set on aware-buzzard-568: BETTER_AUTH_{SECRET,ISSUER,JWKS_URL,URL,TRUSTED_ORIGINS} (mirrors crm).
  - Better-Auth password min length > 8 (signup with "12345678" → PASSWORD_TOO_SHORT). Use ≥12-char test passwords.
- **P1 — DONE** (verified, no code change). Live signup `POST aware-buzzard-568.convex.site/api/auth/sign-up/email` → sync trigger → Seal `users` row created with clerkId=BA-subject + vortexAuthUserId=component-userId, name/locale/timezone defaulted. Test user `seal-p1-bridge@seal.nyc` left on dev. Both providers coexist in deployed auth config.

## P2 sub-step breakdown (component-truth) — IN PROGRESS
crm splits component-truth across writers + readers + auth-rewire + backfill. Seal mirrors it. Sub-steps:
- **P2a — DONE** (commit `af47d6e`): `lib/vortexAuthOrganizations.ts` — component WRITE helpers (ensureVortexAuthOrganization/SystemRoles, ensureComponentRoleForTemplate, upsertVortexAuthMember, createVortexAuthInvitation, set/record invitation status). Uses Seal `ROLE_PERMISSIONS` + org anchor fields. API-key writers omitted (P5). Additive, no call site. tsc clean.
- **P2b — DONE** (commit `eac46d0`): `lib/componentOrgReads.ts` — component READ helpers (memberships, members-by-org, member-by-id, member-ref, role readers, invitation readers byTokenHash/byEmailId/listByOrg). Maps component ids→Seal anchors. API-key readers omitted (P5). **Seal codegen exposes component ids as plain `string`, NOT branded GenericId like crm** — important when copying more crm code. Additive, no call site. tsc + lint clean.
- **P2c — DONE** (commit `4a8bd49`): `auth.ts getAuthContext` now DUAL-READS — component membership owns role+status when present (local row preserved for app fields; synthesized post-P7), else local fallback; mutation contexts lazily mirror local→component via `upsertVortexAuthMember` once the user is bridged. Clerk-only users short-circuit to local (zero component queries) → app stays 100% on Clerk until P4. Verified: tsc/lint clean, 1368/1368 tests, + LIVE round-trip proof on dev (write component membership → dual-read returns owner/active, member id matches).
- **P2d — DEFERRED to P7 (deliberate, not skipped).** Eager invitation/membership dual-write on create is REDUNDANT with P2e(2) lazy materialization: the component becomes the membership truth whenever the user next authenticates with a `vortexAuthUserId`, regardless of how the membership originated (signup / direct-add / invite-accept). The only thing invitation-component-storage adds is component-OWNED pending-invite *listing*, which is only needed when the local `organization_invitations` table is dropped (P7). So route `createInvitation`→`createVortexAuthInvitation` (+ sha256 tokenHash) + accept-flow→component AT P7 alongside the table drop. Seal's `createInvitation` already uses `crypto.randomUUID()` (the "token=Clerk-invite-id" issue was only the clerk_webhooks sync path). crm refs: `organizationInvitations.ts` (sha256 at :398, redeem at :161-283).
- **P2e — backfill existing Clerk-synced data into component.** ⚠️ **CHICKEN-EGG (the crux/risk):** putting a member in the component needs the user's `vortexAuthUserId` (component identity), which existing Clerk-only users DON'T have until they sign in via Better-Auth (`users.upsertFromBetterAuth` provisions it).
  - **P2e(1) — DONE** (commit `5658fa4`): `migrations.backfillOrganizationComponentAnchors` anchors every org into the component (sets `vortexAuthOrganizationId`) + seeds `ROLE_PERMISSIONS`. Idempotent, no user identity needed. RAN + VERIFIED on dev `aware-buzzard-568` (2 orgs anchored: Vortex→`kd71g23m…`, Seal CI E2E→`kd7a4azn…`). Re-run anytime: `bunx convex run migrations:run '{"fn":"migrations:backfillOrganizationComponentAnchors"}'` (needs `NODE_AUTH_TOKEN=$(gh auth token)`).
  - **P2e(2) — TODO: membership population.** Decision: populate lazily — when a user first authenticates via Better-Auth (post-P4) the glue/auth-context calls an `ensureComponentMembership(user, activeOrg)` path that mirrors their local `organization_members` row(s) into the component via `upsertVortexAuthMember` (now that they have `vortexAuthUserId`). NO bulk membership backfill (can't — no identities). Re-read crm `auth.ts:489-522`.
- **P2c — switch the auth hot path to the glue.** Make `auth.ts getAuthContext` (line ~236) delegate to `canonicalAuth.resolveViewer(ctx)` (crm `auth.ts:213-228 getAuthContextWithPermissions`). Read `users.activeVortexAuthOrganizationId` + org `vortexAuthOrganizationId`. **MUST come AFTER P2e** — switching reads before memberships exist in the component breaks every authenticated request for existing users. Test live on `aware-buzzard-568` before/after.

## P4 — DONE (commit `99fd25b`), verified end-to-end live
Atomic web cutover landed + browser-proven on dev (vite, signup→auth→org-chooser→create-workspace→authenticated `/{slug}/home` shell with Owner role via dual-read, ZERO console errors). 25 files. Pre-built `runtime.Auth*RoutePage` for sign-in/up/accept-invite; `useAppAuth` gating in `_authenticated`/`app` (dropped Clerk org-sync recovery); `hooks/use-current-user.ts` adapter over `useAppUser` (Clerk-shaped; consumers swap via import alias); `useAppAuthActions().signOut`; deleted clerk-auth-theme + waitlist route.
- **P4 FOLLOW-UPS (not blockers):** (1) profile/security pages are MINIMAL — full account-management + 2FA enroll/step-up UI via `vortex-auth/react` components is TODO. (2) accept-invite renders sign-in (token preserved); full component-side redemption lands with P7 invitation routing. (3) **`BETTER_AUTH_TRUSTED_ORIGINS` env is AUTHORITATIVE for CORS** (not the betterAuth.ts config) — set it per-deployment to the real staging/prod app origins (dev currently includes `localhost:4240,seal.localhost:1355`; the `localhost:*` config addition is for CSRF/dev convenience). (4) portless was 502ing in the test env; tested via direct vite port — confirm via portless when it's healthy.

## P5 — DONE (commits `51e38a0`,`6b5affa`,`48d56db`), live-verified
API keys fully on the vortexAuth component (Clerk out of the API-key path):
- `lib/vortexAuthOrganizations.ts` + `lib/componentOrgReads.ts`: component api-key write/read helpers
- `api_keys/keys.ts`: createApiKey/listApiKeys/revokeApiKey (permission-gated via dual-read auth) + internal getApiKeyByPrefix
- `api/context.ts` resolveApiAuth → `resolveStoredApiKeyCredential` (component verify); removed Clerk api-key cluster (verifyApiKey/resolveOrg|UserKeySubject/getClerkClient/createClerkClient import)
- frontend `settings/developer/api-keys.tsx` → new mutations/query
- VERIFIED live: created a component key → API request reached the plan gate (403, not 401), proving component verify resolved it. tsc/lint/1368 tests green.
- P5 NOTE: the JWT/OAuth path in api/context.ts still uses Clerk `verifyToken` (MCP) — torn out in P6.

## P6 — DONE (dead-code teardown, commit `8c4a645`), verified
Removed the now-dead Clerk surface from the Seal app: DELETED `clerk_webhooks.ts` (~1033L) + its test; stripped the `/clerk-webhooks` http route + all handlers/types/helpers from `http.ts` (kept Stripe + MCP-oauth + REST routes); removed dead `recoverOrganizationSyncFromClerk`/`setActiveOrganizationFromClerk` from `check_membership.ts`; removed Clerk→Convex backfill actions from `sync_external_data.ts` (kept Stripe sync); `auth.config.ts` now Better-Auth-provider-only. tsc/lint/1359 tests green; `convex dev --once` deploys clean.
- **P6 REMAINDER folded into P7/P8 (entangled, NOT yet removed):** `organizations/actions.ts` Clerk invite actions (need P7 invitation-component routing first), the JWT/OAuth Clerk path in `api/context.ts` (`verifyToken`/`verifyOAuthAccessToken` — serves MCP; needs MCP→Better-Auth or a session-token path), old `api_keys/actions.ts`, `@clerk/backend` dep (used by those), `CLERK_*` env, frontend `@clerk/*` deps (`@clerk/testing` used by e2e — rewrite e2e in P8; `@clerk/clerk-react`/`@clerk/themes` now import-free but kept until e2e migrates), dead `svix` backend dep.

## Resume
Read this file + memory `project-seal-migration-plan.md`. Branch `feat/vortex-auth-p0-scaffold` (commits through `8c4a645`). **P0–P6 DONE + LIVE-VERIFIED — Clerk is dethroned everywhere functionally (auth runtime, orgs/members, email, frontend, API keys all on vortex-auth); the dead Clerk webhook/sync code is removed.**
### P7a — DONE (invitations on the component; commits `4e63700`,`cf8024a`), live-proven
P6 had SEVERED invites (deleted the Clerk acceptance webhook). Rebuilt on the component: `convex/invitations.ts` (createInvitation+UUID/sha256+scheduled invite email via `createOrganizationInvitationEmailDraft`+Resend; listInvitations; revokeInvitation; public getInvitationByToken; redeemInvitation→component membership+active org, resolves user via identity since invitee has no org yet). Frontend: invite-member-dialog→`createInvitation`, accept-invite page→reads `?token`, redeems when signed-in else renders sign-up returning to the token. **Live-proven** (create→token-lookup `pending`→redeem→membership `member/active`→invitation `accepted`). tsc/lint/1359 tests green.
- **P7a — FULLY DONE** (commits through `9844d9e`): pending-list + team page rewired to `api.invitations.*`; removed dead Clerk invite backend (clerkInvite/clerkRevokeInvitation/getInvitationEmailByClerkId/clerkResendInvitation, getPendingInvitations/getInvitationById/getInvitationByClerkId, old local createInvitation/cancelInvitation). Invitations 100% on the component, no Clerk.
- **P7 (schema) — PARTIAL DONE:** dropped (strip-then-drop migrations, live) `organization_members.clerkMembershipId`/`externalId` + `by_clerk_membership_id` (commit `9ae27f3`); `organization_invitations.clerkInvitationId`/`clerkOrganizationId` + `by_clerk_invitation_id` (commit `9844d9e`).

**Remaining P7 — the risky/cross-app tail (NONE block function; Clerk is gone from all features):**
2. **MCP / JWT path**: migrate `api/context.ts` `resolveJwtAuth`/`verifyOAuthAccessToken` off Clerk `verifyToken` (Better-Auth session/bearer) — coordinate with mcp-server/worker; this is the last `@clerk/backend` user in the Seal app.
3. **`users.clerkId` + `organizations.clerkId` drop** — the LARGE one (~15-file identity-key refactor). REFRAME: post-migration `clerkId` is NOT Clerk-coupled — it stores the **Better-Auth subject** (`by_clerk_id`=identity.subject resolves BA users). It's FUNCTIONAL, so dropping it is pure hygiene. To drop: switch every `by_clerk_id`=subject lookup (auth.ts requireAuthenticatedUser, check_membership, invitations, presence, organizations/helpers+mutations) to the component identity 2-hop, AND switch every stored-value consumer (documents/recipients, notifications profile lookup, user_data_export, rls.ts, organizations/queries) off `user.clerkId` to `user._id`/`vortexAuthUserId`, THEN strip-then-drop. Consider just renaming clerkId→`authSubject` instead (cosmetic, lower-risk) since it's functional. + drop waitlist tables.
4. **`clerkDeleteUser`** (organizations/actions.ts, used by team `$memberId.tsx` member-removal) — deletes a Clerk user; obsolete. Rewire member-removal to remove the membership (+ optionally the Better-Auth user) instead.
5. **Dep/env**: `@clerk/backend` (after #2 MCP + #4), `CLERK_*` env, frontend `@clerk/clerk-react`+`@clerk/themes` (import-free now; `@clerk/testing` stays till P8 e2e rewrite), dead `svix`. Also dead `sendTeamInvitationEmail` in `emails/user_email_actions.ts`.

**RECOMMENDATION:** the remaining items are hygiene (#3,#4,#5) or cross-app (#2) — none affect function. Higher value is **P8** (full E2E test + fix `.test-env` + PR→staging) to make the migration shippable, then circle back to the clerkId hygiene refactor + MCP as a focused follow-up.

## P7 — APP-LOCAL CLEANUP COMPLETE (commits through `4d89592`+`@clerk/types` chore)
Did (all live + green, 30 commits/session): dropped dead Clerk FK cols (org_members + invitations, strip-then-drop); invitations 100% on component; deleted old Clerk api_keys/actions.ts; member-removal rewired to component `removeMember` (no Clerk, account-safe); `organizations/actions.ts` DELETED; dropped dead `waitlist_entries` table + schema/rls/seo; removed dead emails (sendWaitlistNotificationEmail, sendTeamInvitationEmail); removed deps svix + `@clerk/clerk-react` + `@clerk/themes` + `@clerk/types`.
**MCP OAuth — SERVER BUILT + DEV-PROVEN (commit `980df0a`); prod cutover remaining:**
- Deleted legacy `apps/mcp-server` (Express+Clerk) + dormant `convex/mcp_oauth` store (commit `3289765`).
- **Built Seal's Better-Auth MCP OAuth authorization server** (mirrors crm, via `@plasmapos/vortex-auth/mcp` + `components.vortexAuth.mcp`): `convex/mcpOAuth.ts`, `mcpOAuthAuthorization.ts`, `mcpOAuthNode.ts` (ES256 signing), `mcpOAuthAuth.ts` (component-backed codes/refresh/clients/keys), `mcpOAuthHttp.ts`, `apiAuth.ts` (BA-user→Seal user/org/perms adapters). `api/context.ts` added `resolveMcpAuth` (isolated `McpAuthContext`) ADDITIVELY. http.ts mounts `/.well-known/oauth-{authorization-server,protected-resource}/seal-mcp` + `/oauth/seal-mcp/{jwks,authorize,register,token}` + `POST /mcp` (tool dispatch — 3 stub tools: get_current_organization/list_documents/list_templates). VERIFIED on dev: metadata + JWKS (auto ES256 key) → 200. tsc/lint/1359 tests green.
- **CUTOVER REMAINING (prod-facing, needs real-MCP-client E2E):** (a) repoint `apps/mcp-worker/src/index.ts` discovery+verify off `@clerk/mcp-tools`/`@clerk/backend.verifyToken` → Seal's `/.well-known/.../seal-mcp` + JWKS (OR retire the worker entirely + port its full tool set from `apps/mcp-worker/src/tools/*` into Seal's `handleMcpRequest`, crm-style — cleaner, kills the worker's Clerk deps); (b) `resolveAuthContext` route MCP bearer tokens → `resolveMcpAuth` instead of the Clerk `resolveJwtAuth`; (c) deploy the OAuth server to PROD `compassionate-robin-742` (worker points there); (d) E2E test the OAuth flow (MCP client → authorize → PKCE → token → tool); (e) THEN delete the Clerk `verifyToken`/`verifyOAuthAccessToken`/`resolveJwtAuth` path + `@clerk/backend` dep + `CLERK_*` env. The Clerk session-JWT branch is likely already dead (web uses BA cookies+Convex, not /api/v1) — only the OAuth/MCP branch is live.
2. **`users.clerkId` + `organizations.clerkId`** — NOT dropped, on purpose. `clerkId` is now the **functional auth-subject identifier** woven through audit logs (`actorId`), notifications (`clerkUserId`), user_profiles (`by_clerk_user_id`), `rls.ts`, document actors, exports — re-keying to `user._id` is a ~30-site, multi-table data-migration RE-ARCHITECTURE for zero functional gain. **Recommend a later cosmetic rename `clerkId`→`authSubject`, not a drop.**
Also still present: web `@clerk/testing` (e2e suite — P8 rewrite); a doc-comment in `hooks/use-current-user.ts` (cosmetic).
4. **Dep/env cleanup**: remove `@clerk/backend` (after 1+2), `CLERK_*` env, frontend `@clerk/clerk-react`/`@clerk/themes`/`@clerk/testing` (after P8 e2e rewrite), dead `svix` backend dep.
Then **P8**: full E2E (signup→org→invite→accept→2FA→API key→sign-out) on dev, rewrite Clerk-based e2e (`apps/web/e2e/*` use `@clerk/testing`), fix stale `.test-env` (self-hosted→Cloud `aware-buzzard-568`), flip `emailVerification.required:true`, Sentry sweep, PR → `staging`.
NOTE: untracked Cloudflare-deploy files + a `git stash` are NOT mine — leave them. NOTE: a separate NOT-MINE workstream (Cloudflare deploy: apps/mcp-worker, wrangler.jsonc, .dockerignore, dist-*) sits UNTRACKED + a `git stash` ("pre-migration: unrelated perf/infra tweaks") holds unrelated tracked edits — leave both alone.
